# 快速修復：音效檔案 404 錯誤

## 問題
網頁顯示「找不到音效檔案」或瀏覽器 Console 顯示 `404 Not Found: /static/big_drum.wav`

## 原因
音效檔案尚未複製到 `static/` 資料夾

## 解決方法（在樹莓派上執行）

### 方法 1: 使用自動修復腳本（推薦）

```bash
cd /path/to/IOT_drum_stick
chmod +x fix_audio.sh
./fix_audio.sh
```

### 方法 2: 手動複製

```bash
cd /path/to/IOT_drum_stick

# 確認 big_drum.wav 存在
ls -lh big_drum.wav

# 複製到 static 資料夾
cp big_drum.wav static/

# 設定權限
chmod 644 static/big_drum.wav

# 確認檔案已複製
ls -lh static/big_drum.wav
```

### 方法 3: 如果 big_drum.wav 在其他位置

```bash
# 找到檔案
find ~ -name "big_drum.wav"

# 複製到正確位置（替換路徑）
cp /找到的/路徑/big_drum.wav /path/to/IOT_drum_stick/static/
```

### 方法 4: 使用其他音效檔案

如果沒有 big_drum.wav，可以使用任何 WAV 格式的音效：

```bash
# 複製其他音效並重新命名
cp your_sound.wav static/big_drum.wav
```

## 完成後

1. **不需要重啟伺服器**（Flask 會自動服務新檔案）
2. **刷新瀏覽器**（Ctrl + Shift + R 或 Cmd + Shift + R）
3. **點擊頁面**啟用音效
4. 應該會看到「✓ 音效已就緒」

## 驗證

在瀏覽器 Console 應該看到：

```
📥 正在載入音效檔案: /static/big_drum.wav
✓ 音效檔案下載成功
✓ 鼓聲音效載入成功
   長度: 0.XX 秒
   採樣率: 44100 Hz
```

## 仍然不行？

檢查檔案結構：

```bash
cd /path/to/IOT_drum_stick
tree -L 2
# 或
ls -lR

# 應該看到：
# ./static/big_drum.wav
```

確認檔案權限：

```bash
ls -la static/big_drum.wav
# 應該是: -rw-r--r--
```

## 直接在瀏覽器測試

在瀏覽器輸入：
```
http://樹莓派IP:5000/static/big_drum.wav
```

- 如果能下載 → Flask 路由正常，檢查 HTML 路徑
- 如果 404 → 檔案不存在
- 如果 403 → 權限問題
